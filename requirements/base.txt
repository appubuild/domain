# ---------------------------------------------------------------------------
# Nova Studio — core runtime dependencies (all platforms)
#
# These are the *compatible ranges* declared in pyproject.toml, resolved to a
# concrete set.  Install with:  pip install -r requirements/base.txt
# Platform-specific extras live in requirements/{windows,linux,macos}.txt.
#
# Policy (ADR-0007): every native media dependency must ship as a wheel that
# bundles its own FFmpeg/OpenCV binaries, so a clean machine with no system
# packages can run Nova Studio.
# ---------------------------------------------------------------------------

# --- Service / API ---------------------------------------------------------
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
orjson>=3.10,<4.0
python-multipart>=0.0.9

# --- Media -----------------------------------------------------------------
# PyAV wheels bundle FFmpeg 7 libraries (libavcodec/libavformat/libavfilter/
# libswscale/libswresample) with libx264, hevc, vp9, aac and libmp3lame.
av>=13.0,<19.0
# Static FFmpeg CLI binary, used by the CLI media backend when no system or
# bundled ffmpeg is present (ADR-0007 §3).
imageio-ffmpeg>=0.5,<1.0
opencv-python-headless>=4.9,<6.0
numpy>=1.26,<3.0
pillow>=10.3,<13.0

# --- Observability / platform ---------------------------------------------
structlog>=24.1,<27.0
watchfiles>=0.21,<1.0
psutil>=5.9,<8.0
typing-extensions>=4.11
